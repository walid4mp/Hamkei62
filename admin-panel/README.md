# SocialNova Admin Panel

Reserved for the moderation/admin dashboard. The API already exposes the core data model needed for users, posts, groups, notifications and live rooms. Build the admin UI separately so regular users never see admin controls.
